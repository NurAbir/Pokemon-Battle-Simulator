# ===========================================
# Pokemon Battle Simulator - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env (Linux/Mac)
# copy .env.example .env (Windows)
# ===========================================

# MongoDB Connection
# Local: mongodb://localhost:27017/pokemon-battle-simulator
# Atlas: mongodb+srv://<username>:<password>@cluster.xxxxx.mongodb.net/pokemon-battle-simulator
MONGODB_URI=mongodb://localhost:27017/pokemon-battle-simulator

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
# Generate a secure key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_key_change_this_in_production

# Server Port
PORT=5000

# Email Configuration (for password reset functionality)
# Use Gmail App Password: https://support.google.com/accounts/answer/185833
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000
